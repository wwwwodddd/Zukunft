#include <bits/stdc++.h>
using namespace std;
int n, m, k;
int main() {
	scanf("%d%d", &n, &k);
	for (int i = 1; i <= n; i++) {
		scanf("%d", &a[i]);
	}
	for (int i = 1; i <= n; i++) {
		scanf("%d", &x);
		a[i] = (3 - 2 * a[i]) * x;
	}
	scanf("%d", &m);
	int L = 1, R = 0;
	for (int i = 0; i < m; i++) {

	}
	for (int i = 0; i < m; i++) {
		printf("%d\n", z[i]);
	}
	return 0;
}